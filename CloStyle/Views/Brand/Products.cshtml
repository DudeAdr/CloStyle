@using CloStyle.Application.CloStyle.Dtos
@model IEnumerable<ProductDto>

@{
    ViewData["Title"] = "Produkty marki";
}

<div class="container mt-4">
    <div class="text-center mb-4">
        <h1 class="display-5">Produkty marki: <span class="text-primary">@ViewData["BrandName"]</span></h1>
    </div>

    @if (!Model.Any())
    {
        <div class="alert alert-warning text-center" role="alert">
            Brak produktów do wyświetlenia.
        </div>
    }
    else
    {
        <div class="table-responsive">
            <table class="table table-bordered table-hover align-middle shadow-sm">
                <thead class="table-dark">
                    <tr>
                        <th>@Html.DisplayNameFor(model => model.First().Name)</th>
                        <th>@Html.DisplayNameFor(model => model.First().Price)</th>
                        <th>@Html.DisplayNameFor(model => model.First().Description)</th>
                        <th>@Html.DisplayNameFor(model => model.First().CategoryName)</th>
                        <th>@Html.DisplayNameFor(model => model.First().GenderName)</th>
                        <th>Rozmiary</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var product in Model)
                    {
                        <tr>
                            <td>@product.Name</td>
                            <td>@product.Price.ToString("C")</td>
                            <td>@product.Description</td>
                            <td>@product.CategoryName</td>
                            <td>@product.GenderName</td>
                            <td>
                                @if (product.Sizes != null && product.Sizes.Any())
                                {
                                    <ul class="list-group list-group-flush">
                                        @foreach (var size in product.Sizes)
                                        {
                                            <li class="list-group-item px-2 py-1">
                                                Rozmiar: <strong>@size.Size</strong> – Ilość: <strong>@size.Stock</strong>
                                            </li>
                                        }
                                    </ul>
                                }
                                else
                                {
                                    <span class="text-muted">Brak danych</span>
                                }
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</div>
